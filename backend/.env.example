# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-dy2la63y(1a!*3evvtb4!&tczirkr$a=1ghe&lkzz^vj-_+=j3

# Allowed Hosts (comma-separated)
# For local development:
ALLOWED_HOSTS=localhost,127.0.0.1
# For production, add your domain and IP:
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,your-server-ip

# Database Configuration
# For local development (SQLite):
DB_ENGINE=sqlite
DB_NAME=db.sqlite3

# For production (PostgreSQL) - uncomment and configure:
# DB_ENGINE=postgresql
# DB_NAME=your_database_name
# DB_USER=your_database_user
# DB_PASSWORD=your_database_password
# DB_HOST=localhost
# DB_PORT=5432

# CORS Settings (comma-separated)
# For local development:
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# For production:
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Email Configuration
EMAIL_BACKEND=console
# For production SMTP (uncomment and configure):
# EMAIL_BACKEND=smtp
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@nexinvo.com
